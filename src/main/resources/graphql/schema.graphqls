type Query{
    taskById(id:ID): Task
    getTasks:[Task]
}

type Mutation{
    createNewTask(text:String,completed:Boolean):Task
    modifyTask(id:Int,text:String,completed:Boolean):Task
    deleteTask(id:Int):Task
}

type Task{
    id: Int
    text: String
    completed: Boolean
}